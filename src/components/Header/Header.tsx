import { HeaderContainer, Title } from './Header.css';

const Header = () => {
  return (
    <HeaderContainer>
      <Title>Conway's Game of Life</Title>
    </HeaderContainer>
  );
};

export default Header;
